/* áp dụng thuật toán fast and slow: rùa và thỏ, duy trì 2 con trỏ cùng xuất phát ở head
mỗi khi di chuyển, fast di chuyển 2, slow di chuyển 1, khi mà fast = nullptr tức là slow ở mid
*/

#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    
    
    return 0;
}